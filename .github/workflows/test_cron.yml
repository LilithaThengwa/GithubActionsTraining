name: test cron

on:
  schedule:
    - cron: '*/5 * * * *'  
  workflow_dispatch:

jobs:
  echo-job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Echo statement
        run: echo "testing testing 123"
